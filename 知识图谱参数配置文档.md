# 知识图谱参数配置文档

## 一、概述

本文档整理了知识图谱功能中所有可调整的参数，包括环境变量、硬编码配置和系统限制。

---

## 二、环境变量配置

### 2.1 图谱应用基础配置

| 环境变量 | 默认值 | 说明 | 文件位置 |
|---------|-------|------|---------|
| `APP_NAME` | `Tree Graph System` | 应用名称 | `graph/modules/Graph/backend/config.py:7` |
| `FLASK_DEBUG` | `true` | Flask 调试模式 | `graph/modules/Graph/backend/config.py:8` |
| `APP_HOST` | `127.0.0.1` | 应用监听地址 | `graph/modules/Graph/backend/config.py:9` |
| `APP_PORT` | `5002` | 应用监听端口 | `graph/modules/Graph/backend/config.py:10` |

### 2.2 外部图谱 API 配置

| 环境变量 | 默认值 | 说明 | 文件位置 |
|---------|-------|------|---------|
| `EXTERNAL_GRAPH_API_BASE` | `http://127.0.0.1:62272/v1` | 外部图谱 API 基础地址 | `graph/app.py:34` |
| `EXTERNAL_GRAPH_MODEL_PATH` | `Qwen3.5-27B` | 外部图谱使用的模型路径 | `graph/app.py:35` |
| `EXTERNAL_GRAPH_EXTRACT_TIMEOUT_SECONDS` | `180` | 图谱提取超时时间（秒） | `graph/app.py:36` |

### 2.3 数据库连接配置（内部图谱）

| 环境变量 | 默认值 | 说明 | 文件位置 |
|---------|-------|------|---------|
| `DB_HOST` | `183.69.138.62` | 数据库主机 | `graph/modules/Graph/backend/config.py:12` |
| `DB_PORT` | `33666` | 数据库端口 | `graph/modules/Graph/backend/config.py:13` |
| `DB_USER` | `hagongda` | 数据库用户名 | `graph/modules/Graph/backend/config.py:14` |
| `DB_PASSWORD` | `ha.G/o[tEst]n%gD*a` | 数据库密码 | `graph/modules/Graph/backend/config.py:15` |
| `DB_NAME` | `r_d_test` | 数据库名称 | `graph/modules/Graph/backend/config.py:16` |
| `DB_CHARSET` | `utf8mb4` | 数据库字符集 | `graph/modules/Graph/backend/config.py:17` |

### 2.4 图谱提取模型配置（getgraph）

| 环境变量 | 默认值 | 说明 | 文件位置 |
|---------|-------|------|---------|
| `GETGRAPH_API_BASE` | - | getgraph 模块的 API 基础地址（如未设置则使用命令行参数） | `graph/modules/xintuoyin/getgraph/main.py:1244` |
| `GETGRAPH_LLM_TIMEOUT` | `120` | LLM 调用超时时间（秒） | `graph/modules/xintuoyin/getgraph/main.py:1258` |
| `GETGRAPH_LLM_RETRIES` | `3` | LLM 调用重试次数 | `graph/modules/xintuoyin/getgraph/main.py:1265` |
| `GETGRAPH_LLM_RETRY_SLEEP_SECONDS` | `2` | LLM 调用重试间隔（秒） | `graph/modules/xintuoyin/getgraph/main.py:1266` |
| `GETGRAPH_CATEGORY_MAX_TOKENS` | `64` | 文件分类时的最大 Token 数 | `graph/modules/xintuoyin/getgraph/main.py:461` |
| `GETGRAPH_EXTRACTION_MAX_TOKENS` | `4096` | 实体关系提取时的最大 Token 数 | `graph/modules/xintuoyin/getgraph/main.py:1325` |

### 2.5 OCR 相关配置

| 环境变量 | 默认值 | 说明 | 文件位置 |
|---------|-------|------|---------|
| `IMAGE_OCR_GPU_MIN_FREE_MB` | `4096` | 图片 OCR 使用 GPU 的最小空闲显存（MB） | `graph/modules/xintuoyin/getgraph/main.py:84` |
| `IMAGE_DIRECT_OCR_MIN_TEXT_LEN` | `20` | 直接使用 RapidOCR 的最小文本长度 | `graph/modules/xintuoyin/getgraph/main.py:85` |

### 2.6 缓存配置

| 环境变量 | 默认值 | 说明 | 文件位置 |
|---------|-------|------|---------|
| `GRAPH_CACHE_TTL_SECONDS` | `60` | 图谱缓存 TTL（秒） | `graph/modules/Graph/backend/services/graph_service.py:15` |

---

## 三、硬编码文件类型限制

### 3.1 支持的文件扩展名

| 文件类型 | 支持的扩展名 | 说明 | 文件位置 |
|---------|------------|------|---------|
| 图片文件 | `.jpg`, `.jpeg`, `.png`, `.bmp`, `.tif`, `.tiff`, `.webp` | 支持 OCR 识别 | `graph/modules/xintuoyin/getgraph/main.py:78` |
| PDF 文件 | `.pdf` | 支持文本提取和 OCR | `graph/modules/xintuoyin/getgraph/main.py:79` |
| 文本文件 | `.txt` | 直接读取 | `graph/modules/xintuoyin/getgraph/main.py:80` |
| 旧版 Excel | `.xls` | 需通过 LibreOffice 转换为 `.xlsx` | `graph/modules/xintuoyin/getgraph/main.py:81` |
| 新版 Excel | `.xlsx` | 直接处理 | `graph/modules/xintuoyin/getgraph/main.py:82` |
| 表格文件 | `.csv` | 包含在 `TABULAR_EXTENSIONS` 中 | `graph/modules/xintuoyin/getgraph/main.py:83` |

### 3.2 业务文件类别（支持自动识别）

系统支持自动识别以下 35 类业务文件：

| 文件类别 | 说明 |
|---------|------|
| 研发项目汇总表 | 有主键的文件 |
| 研发费用加计扣除优惠明细表 | Excel 表格文件 |
| 研发支出辅助帐汇总表 | 有主键的文件 |
| 研发支出辅助帐-项目名称 | Excel 表格文件 |
| 企业研发项目情况表 | 有主键的文件 |
| 企业研发活动及相关情况表 | Excel 表格文件 |
| 委托合同 | 用户上传文件 |
| 委托费用明细表 | Excel 表格文件 |
| 项目立项决议书 | 用户上传文件 |
| 项目立项报告 | 用户上传文件 |
| 项目计划任务书 | 用户上传文件 |
| 鉴定意见 | 用户上传文件（别名：鉴定建议、验收意见、专家评审报告） |
| 科技部门登记合同 | 用户上传文件 |
| 检索记录 | 用户上传文件 |
| 论证记录 | 用户上传文件 |
| 调研记录 | 用户上传文件（别名：调研报告、调研纪要、调研材料） |
| 成果附件 | 用户上传文件 |
| 结题报告 | 用户上传文件 |
| 研发人员名单 | 有主键的文件（以工号为主键） |
| 人员人工费用明细表 | 以工号为主键 |
| 社保补缴 | 以工号为主键 |
| 人员工时记录 | 以工号为主键 |
| 直接投入费用明细 | Excel 表格文件 |
| 材料领用 | Excel 表格文件 |
| 燃料领用 | Excel 表格文件 |
| 动力消耗明细 | Excel 表格文件 |
| 设备租金费用明细表 | 以设备编号为主键 |
| 设备工时记录 | 以设备编号为主键 |
| 设备折旧费用明细表 | 以设备编号为主键 |
| 无形资产工时记录 | 以无形资产为主键 |
| 无形资产摊销费用明细表 | 以无形资产为主键 |
| 新产品设计费 | Excel 表格文件 |
| 其他费用明细表 | Excel 表格文件 |
| 补充养老保险费用明细 | 以工号为主键 |
| 补充医疗保险费用明细 | 以工号为主键 |
| 职工福利费用明细 | 以工号为主键 |
| 发票 | 用户上传文件 |

**文件位置**：`graph/modules/xintuoyin/getgraph/main.py:24-57`

---

## 四、文件大小和长度限制

目前代码中**没有明确的文件大小硬编码限制**，但有以下相关限制：

| 限制项 | 说明 | 相关代码位置 |
|-------|------|------------|
| 提取超时 | `EXTERNAL_GRAPH_EXTRACT_TIMEOUT_SECONDS` 环境变量控制（默认 180 秒） | `graph/app.py:36, 515` |
| LLM 超时 | `GETGRAPH_LLM_TIMEOUT` 环境变量控制（默认 120 秒） | `graph/modules/xintuoyin/getgraph/main.py:1258` |
| OCR 文本长度 | 直接 RapidOCR 模式需要至少 20 个字符（可通过 `IMAGE_DIRECT_OCR_MIN_TEXT_LEN` 调整） | `graph/modules/xintuoyin/getgraph/main.py:85, 372` |
| GPU 显存要求 | 使用 GPU OCR 需要至少 4096MB 空闲显存（可通过 `IMAGE_OCR_GPU_MIN_FREE_MB` 调整） | `graph/modules/xintuoyin/getgraph/main.py:84, 313` |

---

## 五、模型配置

### 5.1 使用的模型

| 用途 | 默认模型 | 配置方式 | 文件位置 |
|-----|---------|---------|---------|
| 外部图谱实体/关系提取 | `Qwen3.5-27B` | `EXTERNAL_GRAPH_MODEL_PATH` 环境变量 | `graph/app.py:35` |
| 文件分类 | 与 `EXTERNAL_GRAPH_MODEL_PATH` 相同 | 通过命令行 `--model_path` 参数传递 | `graph/modules/xintuoyin/getgraph/main.py:454` |
| 实体关系提取 | 与 `EXTERNAL_GRAPH_MODEL_PATH` 相同 | 通过命令行 `--model_path` 参数传递 | `graph/modules/xintuoyin/getgraph/main.py:1320` |

### 5.2 模型调用参数

| 参数 | 默认值 | 用途 | 文件位置 |
|-----|-------|------|---------|
| temperature | 0 | 文件分类时（固定值） | `graph/modules/xintuoyin/getgraph/main.py:460` |
| max_tokens | 64 | 文件分类响应长度 | `graph/modules/xintuoyin/getgraph/main.py:461` |
| max_tokens | 4096 | 实体关系提取响应长度 | `graph/modules/xintuoyin/getgraph/main.py:1325` |

---

## 六、数据库连接配置

### 6.1 内部图谱 SQL 数据库

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| 主机 | `183.69.138.62` | MySQL 服务器地址 |
| 端口 | `33666` | MySQL 端口 |
| 用户名 | `hagongda` | 数据库用户名 |
| 密码 | `ha.G/o[tEst]n%gD*a` | 数据库密码 |
| 数据库名 | `r_d_test` | 业务数据库名称 |
| 字符集 | `utf8mb4` | 数据库连接字符集 |

**环境变量**：`DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `DB_CHARSET`

**文件位置**：`graph/modules/Graph/backend/config.py:12-17`

### 6.2 图谱数据存储位置

| 存储类型 | 路径 | 说明 |
|---------|------|------|
| 外部图谱 JSON | `graph/runtime/external_workspace/<graphId>.json` | 用户创建的外部图谱 |
| 内部绑定图谱 | `graph/runtime/internal_binding_workspace/internal_binding_graph.json` | 与内部 SQL 图谱绑定的图谱 |
| 上传文件 | `graph/runtime/external_workspace/uploads/` | 外部图谱上传的文件 |
| 内部绑定上传 | `graph/runtime/internal_binding_workspace/uploads/` | 内部绑定图谱上传的文件 |
| 工作区元数据 | `graph/runtime/external_workspace/workspace_meta.json` | 外部图谱工作区元数据 |
| 图谱索引 | `graph/runtime/external_workspace/graph_index.json` | 外部图谱列表索引 |

**文件位置**：`graph/app.py:13-28`

---

## 七、其他配置

### 7.1 实体分组映射

系统将实体按类型分组，用于图谱可视化：

| 实体类型 | 分组 ID |
|---------|--------|
| 负责人、参与人员、项目成员、协作接口人、专家姓名、开票人、复核人、收款人、主持人、记录人、参与调研人员、项目联系人、姓名、法定代表人 | 1 |
| 甲方（委托方）、乙方（受托方）、协作单位、专家名单、单位/职称、购买方、销售方、委托方/受托方信息、执行单位/团队 | 2 |
| 项目名称、成果名称、调研主题、论证事项、发票类型 | 3 |
| 其他实体（不可抗力、项目单位、部门等） | 4 |

**文件位置**：`graph/modules/xintuoyin/getgraph/main.py:59-72`

### 7.2 代理配置

| 配置项 | 说明 |
|-------|------|
| NO_PROXY / no_proxy | 不使用代理的地址，默认包含 `127.0.0.1`, `localhost`, `10.249.40.204` |

**文件位置**：`graph/app.py:503-509`

---

## 八、配置示例

### 8.1 完整环境变量配置示例

```bash
# 应用基础配置
export APP_NAME="研发猫知识图谱"
export FLASK_DEBUG="false"
export APP_HOST="0.0.0.0"
export APP_PORT="5002"

# 外部图谱 API
export EXTERNAL_GRAPH_API_BASE="http://your-vllm-server:8000/v1"
export EXTERNAL_GRAPH_MODEL_PATH="Qwen3.5-27B"
export EXTERNAL_GRAPH_EXTRACT_TIMEOUT_SECONDS="300"

# 数据库连接
export DB_HOST="your-mysql-host"
export DB_PORT="3306"
export DB_USER="your-username"
export DB_PASSWORD="your-password"
export DB_NAME="your-database"
export DB_CHARSET="utf8mb4"

# 图谱提取模型
export GETGRAPH_API_BASE="http://your-vllm-server:8000/v1"
export GETGRAPH_LLM_TIMEOUT="180"
export GETGRAPH_LLM_RETRIES="5"
export GETGRAPH_LLM_RETRY_SLEEP_SECONDS="3"
export GETGRAPH_CATEGORY_MAX_TOKENS="128"
export GETGRAPH_EXTRACTION_MAX_TOKENS="8192"

# OCR 配置
export IMAGE_OCR_GPU_MIN_FREE_MB="8192"
export IMAGE_DIRECT_OCR_MIN_TEXT_LEN="50"

# 缓存配置
export GRAPH_CACHE_TTL_SECONDS="120"
```

---

## 九、配置文件位置汇总

| 配置文件 | 说明 |
|---------|------|
| `backend/script/setting.sh` | 后端整体环境变量配置 |
| `graph/app.py` | 图谱主应用入口和外部图谱配置 |
| `graph/modules/Graph/backend/config.py` | 内部图谱 Flask 应用配置 |
| `graph/modules/xintuoyin/getgraph/main.py` | 图谱提取核心逻辑和参数 |
| `graph/modules/Graph/backend/services/graph_service.py` | 图谱服务缓存配置 |

---

## 十、注意事项

1. **文件大小**：虽然没有硬编码的文件大小限制，但受限于：
   - 提取超时时间（`EXTERNAL_GRAPH_EXTRACT_TIMEOUT_SECONDS`）
   - LLM 调用超时时间（`GETGRAPH_LLM_TIMEOUT`）
   - GPU 显存大小（OCR 阶段）

2. **模型切换**：切换模型时需要同时修改：
   - `EXTERNAL_GRAPH_MODEL_PATH` 环境变量
   - 确保 vLLM 服务端已加载对应模型

3. **数据库连接**：内部图谱依赖 MySQL 数据库，需确保连接参数正确

4. **OCR 模式**：图片 OCR 会自动选择 GPU 或 CPU，取决于空闲显存是否达到 `IMAGE_OCR_GPU_MIN_FREE_MB` 阈值
