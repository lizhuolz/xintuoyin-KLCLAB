### 有一些接口的逻辑需要更改，在需要前端测试时的字段后面加入描述，比如query path body等，目前导入apipost后面描述是undefined
，path1. 历史记录获取，反馈列表获取，知识库获取 这类的接口缺少分页参数：页码，page；分页数量，size，按照10条/页处理
2. 创建知识库接口中的category由于知识库的逻辑更改了，不再区分企业-部门-个人这个逻辑，所以删除掉这个字段
3. 更新知识库这个接口有大问题，甲方说没办法同时删除文件和上传文件，以及没办法实现取消后文件不动，点击确定之后才实现完整的上传或删除逻辑。
4. 查询历史记录列表这个接口少了一些功能，需要多几个字段，1.该对话最后一轮的用户输入2.最后一轮对话用户得到的回答，user中添加用户的RecordID和ip地址两个字段
5. 查询反馈列表中的返回的字段有些混乱，需要重构一下，把user的信息（在user.json中）写成user:{}的形式，有4个time的字段，分析一下区别，需要加一个反馈类型的字段，因为之前的type只有like和dislike两种，新加入的这个字段要包含：全部，针对问题，针对回答效果，举报，点赞，点踩这个几个值，（或者把这个也写成类似user:{}的形式）方便前端筛选
6. 数据库选择缺少显式的字段来选，后端中肯定有这个逻辑，找一下，用一个接口返回